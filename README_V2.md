# Valentine Agency - V2 Redesign & Database Integration

## 🚀 Новые фичи (V2)
- **Premium Dark Design**: Полностью темная тема с акцентами золота (CSS Variables в `globals.css`).
- **Supabase Auth Middleware**: Защита маршрутов и сессий (`src/middleware.ts`).
- **Server Actions v2**: Валидация всех данных через Zod (`src/lib/validations.ts`).
- **Typed Database**: Полная типизация Supabase клиента (`src/types/database.types.ts`).
- **UI Kit**: Вынесенные компоненты `FloatingInput` и `FloatingSelect`.
- **SEO**: Добавлены `sitemap.xml`, `robots.txt`, Open Graph метатеги.

## 🛠 Настройка базы данных (Supabase)

Для работы формы убедитесь, что в Supabase создана таблица `leads`:

```sql
create table public.leads (
  id bigint generated by default as identity not null,
  created_at timestamp with time zone not null default now(),
  name text not null,
  telegram text not null,
  age text not null,
  shift_preference text not null, -- 'morning', 'day', 'evening'
  device text not null, -- 'phone', 'laptop', 'both'
  status text not null default 'new'::text,
  constraint leads_pkey primary key (id)
);

-- Отключаем RLS для публичной вставки (или настраиваем политику)
alter table public.leads enable row level security;

create policy "Enable insert for everyone"
on public.leads
for insert
to public
with check (true);
```

## 📦 Сборка и запуск

```bash
npm run dev
# или
npm run build
npm start
```
